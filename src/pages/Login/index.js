import React, {Component} from 'react';
import classNames from 'classnames';
import styles from './styles.less';

class Login extends Component {
    render() {
        return (
            <div className={styles.font}>Hello World</div>
        );
    }
}

export default Login;
